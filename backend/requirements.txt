# ============================================================================
# 开发环境依赖（宽松版本 >=，便于升级探索）
# 生产部署请勿使用本文件 —— 版本不可复现，存在供应链风险。
# 生产部署请使用精确锁版本：pip install -r requirements.lock
# ============================================================================

langgraph>=0.2.0
langchain-openai>=0.2.0
python-dotenv>=1.0.0
PyYAML>=6.0
fastmcp>=2.0.0
websockets>=15.0.1
mcp>=1.0.0
langchain-mcp-adapters>=0.1.0
mcp-server-time
mcp-server-fetch
# 向量数据库：生产默认用 milvus（pymilvus），chromadb 仅作为可选备选后端
pymilvus>=2.3.0
# chromadb: 可选备选向量后端，生产默认用 milvus；切换为 chromadb 时需 pip install chromadb
chromadb>=0.4.0
openai>=1.0.0
#flask>=2.0.0
pandas>=1.3.0
# Keep the FastAPI stack reproducible across GitHub Actions and cloud builds.
fastapi==0.136.1
starlette==1.0.0
pydantic==2.13.3
uvicorn>=0.15.0
sse-starlette>=1.6.0
sentence-transformers>=2.2.0
# numpy: 业务代码（rag/runtime）直接 import，显式声明避免仅靠 pandas 传递依赖
numpy>=1.24.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# Optional PostgreSQL dependencies for future migration
# Uncomment when DATABASE_URL is configured
psycopg2-binary>=2.9.0
SQLAlchemy>=2.0.0

# Structured logging
structlog>=23.0.0
prometheus-client>=0.20.0

# Google A2A Protocol for Agent-to-Agent communication
# google-a2a>=0.1.0

# Redis for message queue
redis>=5.0.0
hiredis>=2.2.0

# Celery for async task queue
celery>=5.5.0
gevent>=24.0.0

# LlamaIndex: 对照实现（RAG-Anything 风格）使用，与自研 HybridRAG 互为参照
llama-index>=0.10.0
llama-index-vector-stores-milvus>=0.1.0

# Test dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.24.0  # for FastAPI TestClient async
